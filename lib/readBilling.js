/**
 * insert billing data into MongoDB model
 * @param zip An instance of JSZip containing the csv files (-> cae, res, lvr) like those from https://cmisrvm1.epfl.ch/cmi/v1.5/copernic_2/
 * @param mongooseCnx A Mongoose connection to load the data into
 */

module.exports = function readBilling (zip, mongooseCnx) {

    throw new Error('unimplemented');

};
